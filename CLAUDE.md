# Playground

This repo is a scratch pad for random experiments. Treat it as a playground.

## Conventions

- Each experiment, scratch, or session lives in its own top-level folder.
- A folder may later be extracted into its own separate repo, or stay here indefinitely — assume nothing about longevity.
- Keep each folder self-contained (its own build, deps, notes) so extraction is cheap.
